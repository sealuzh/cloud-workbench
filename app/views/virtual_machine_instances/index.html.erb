<% vms = 'Virtual Machines' %>
<% provide(:title, vms) %>
<% content_header(vms) %>

<%= will_paginate @virtual_machine_instances %>
<%= render 'shared/virtual_machine_instances', show_benchmark_definition: true %>
<%= will_paginate @virtual_machine_instances %>
