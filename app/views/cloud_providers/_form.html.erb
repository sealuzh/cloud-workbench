<%= form_for(@cloud_provider) do |f| %>
  <% if @cloud_provider.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@cloud_provider.errors.count, "error") %> prohibited this cloud_provider from being saved:</h2>

      <ul>
      <% @cloud_provider.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :credentials_path %><br>
    <%= f.text_field :credentials_path %>
  </div>
  <div class="field">
    <%= f.label :ssh_key_path %><br>
    <%= f.text_field :ssh_key_path %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
