<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=Edge,chrome=1">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= content_for?(:title) ? yield(:title) : "Cloud WorkBench" %></title>
    <%= csrf_meta_tags %>

    <%= stylesheet_link_tag    "application", media: "all", "data-turbolinks-track" => true %>
    <%= javascript_include_tag "application", "data-turbolinks-track" => true %>

    <!-- Le HTML5 shim, for IE6-8 support of HTML elements -->
    <!--[if lt IE 9]>
      <script src="//cdnjs.cloudflare.com/ajax/libs/html5shiv/3.6.1/html5shiv.js" type="text/javascript"></script>
    <![endif]-->

    <!-- Size should be 32 x 32 pixels -->
    <%= favicon_link_tag 'favicon.ico', :rel => 'shortcut icon' %>
  </head>


  <!-- Overwrite all stylesheets min-heights to ensure that dropdowns work properly -->
  <body class="skin-blue pace-done" style="min-height: 2114px;">

  <!--Page loading progress bar-->
  <div class="pace  pace-inactive"><div class="pace-progress" data-progress-text="100%" data-progress="99" style="width: 100%;">
    <div class="pace-progress-inner"></div>
  </div>
  <div class="pace-activity"></div></div>

   <!-- Top header -->
  <header class="header">
    <!-- Branding -->
    <%= link_to root_path, class: "logo" do %>
      <i class="brand-icon fa fa-cloud fa-2x"></i><span class="brand">&nbsp;WorkBench</span>
    <% end %>

    <!-- Top navigation bar -->
    <nav class="navbar navbar-static-top" role="navigation">
      <!-- Sidebar toggle button right to the branding -->
      <a href="#" class="navbar-btn sidebar-toggle" data-toggle="offcanvas" role="button">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </a>

      <!-- Top right navigation bar -->
      <div class="navbar-right">
        <ul class="nav navbar-nav">
          <%= render 'layouts/notifications_example' %>
        </ul>
      </div>
    </nav>
  </header>


  <div class="wrapper row-offcanvas row-offcanvas-left" style="min-height: 319px;">
    <!-- Left sidebar -->
    <aside class="left-side sidebar-offcanvas" style="min-height: 2114px;">
      <section class="sidebar">
        <%= render 'layouts/sidebar' %>
      </section>
    </aside>

    <!-- Main content -->
    <aside class="right-side">
      <section class="content-header">
        <!-- Each view should provide a content header -->
        <%= yield :content_header %>
      </section>
      <section class="content">
        <%= render 'layouts/bootstrap_flash', flash: flash %>
        <%= yield %>
      </section>
    </aside>
  </div>

  <%= render 'layouts/footer' %>

  <% if Rails.env.development? %>
  <div class="well">
    <h4>Debugging</h4>
    <%= debug(params) %>
  </div>
  <% end %>

  </body>
</html>
