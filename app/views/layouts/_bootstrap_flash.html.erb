<% flash.each do |key, value| %>
  <div class="alert alert-<%= flash_class(key) %> alert-dismissable">
    <button type="button" class="close" data-dismiss="alert" aria-hidden="true">×</button>
    <%= sanitize(value.to_s) %>
    <%= flash_icon(key) %>
  </div>
<% end %>
