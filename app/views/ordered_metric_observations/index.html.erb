<h1>Listing ordered_metric_observations</h1>

<table>
  <thead>
  <tr>
    <th>Metric definition id </th>
    <th>Virtual machine instance id</th>
    <th>Time</th>
    <th>Value</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>
  </thead>

  <tbody>
  <% @ordered_metric_observations.each do |ordered_metric_observation| %>
    <tr>
      <td><%= ordered_metric_observation.metric_definition_id %></td>
      <td><%= ordered_metric_observation.virtual_machine_instance_id %></td>
      <td><%= ordered_metric_observation.time %></td>
      <td><%= ordered_metric_observation.value %></td>
      <td><%= link_to 'Show', ordered_metric_observation %></td>
      <td><%= link_to 'Edit', edit_ordered_metric_observation_path(ordered_metric_observation) %></td>
      <td><%= link_to 'Destroy', ordered_metric_observation, method: :delete, data: { confirm: 'Are you sure?' } %></td>
    </tr>
  <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Ordered metric observation', new_ordered_metric_observation_path %>
